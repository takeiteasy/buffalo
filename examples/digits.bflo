# digits.l -- the spec the hand-written examples/digits_tables.c stands in for.
# Used by tests/t_dfa.c: the DFA built from this spec must drive buf_run to the
# same token stream the hand-written M0 table file produces.

%tokens INT

INT    [0-9]+

%skip  [ \t\r\n]+
